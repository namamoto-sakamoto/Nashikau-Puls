<h4>送り先の情報編集</h4>
<%= form_with model:@delivery, url: public_delivery_path, method: :patch, local:true do |f| %>
  <table class="table table-borderless">
    <tr>
      <td><%= f.label :name, "氏名" %></td>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <td><%= f.label :name_kana, "氏名（フリガナ）" %></td>
      <td><%= f.text_field :name_kana %></td>
    </tr>
    <tr>
      <td><%= f.label :postcode, "郵便番号" %></td>
      <td><%= f.text_field :postcode %></td>
    </tr>
    <tr>
      <td><%= f.label :address, "住所" %></td>
      <td><%= f.text_field :address %></td>
    </tr>
    <tr>
      <td><%= f.label :phone_number, "連絡先電話番号" %></td>
      <td><%= f.text_field :phone_number %></td>
    </tr>
  </table>
  <div class="btn btn-outline-info ml-3">
    <%= link_to public_delivery_path(@delivery.customer_id) do %>
      <i class="fa-solid fa-arrow-rotate-left"></i>
     送り先一覧へ戻る
    <% end %>
  </div>
  <%= f.button :type => "submit", class: 'btn btn-success ml-3' do %>
    <div><i class="fa-solid fa-user-plus"></i> 変更を保存 </div>
  <% end %>
<% end %>
