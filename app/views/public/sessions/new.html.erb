<h3>ログイン</h3>
<div class="row">
  <div class="col-8 offset-1">
  <h5><i class="fa-solid fa-apple-whole mr-1"></i>
    いつもNashikau!+をご利用いただきありがとうございます！！
  <i class="fa-solid fa-apple-whole ml-1"></i></h5>
  <%= form_with(model: @farmer, url: session_path(resource_name)) do |f| %>  
    <div class="my-4">
      <%= f.label :"メールアドレス" %><br>
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>
  
    <div class="my-4">
      <%= f.label :"パスワード" %><br>
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>
  
    <% if devise_mapping.rememberable? %>
      <div class="my-4">
        <%= f.check_box :remember_me %>
        <%= f.label :"次回から自動的にログインする" %>
      </div>
    <% end %>
  
    <div>
      <%= f.button :type => "submit", class: 'btn btn-success mt-3' do %>
        <div><i class="fa-solid fa-apple-whole"></i>　ログイン！＋　</div>
      <% end %>  
    </div>
  <% end %>

  <h4 class="mt-5">登録がお済みでない方</h4>
  <p><%= link_to "こちら", new_farmer_registration_path, class: "underline" %>から新規登録を行なってください。</p>
  </div>
</div>  

